// Definição de elementos para seleção de elementos na página.
const elements = {
  menuYou: '#cabecalho > ul > li:nth-child(3) > button > svg',
  menuWhere: ':nth-child(3) > .vr-main-navigation__submenu > :nth-child(2)',
  map: '#map'
};

// Exportar o objeto contendo os seletores para que possa ser utilizado em outros arquivos.
export default elements;
